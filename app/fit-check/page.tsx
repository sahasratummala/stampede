"use client";

import OutfitRecommender from '@/components/outfit-recommender';

export default function FitCheckPage() {
  // We provide an empty object or null to satisfy the TypeScript requirement
  return <OutfitRecommender event={null} />;
}